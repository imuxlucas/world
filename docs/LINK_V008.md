# 顶棚贴图、下接支架与分层眼镜 v008

- 顶棚恢复参考图的八分片色块贴图：左侧粉紫／奶白，右侧蓝／奶白。两张 512×512 PNG 保留于源目录并内嵌 GLB，与实体金属分缝对齐。
- 四根前 A 架和四根后撑的上端分别落在轮毂下方，左右接点分开，不在正面轮毂中心交叉。灯条和灯珠跟随支腿一起调整。
- 删除前脚至后脚的四条落地连接杆；未删除轮毂间已确认的细直横梁。
- 丘比特原始网格及贴图未提供独立镜片。根据贴图颜色与连续网格选择四个镜片前后表面，保留原镜框；增加粉色透明材质和独立眼部、虹膜、眼睑。属于本地几何重建，不是找回原模型隐藏的眼睛。
- 拆件从 9 项增加到 11 项，新增“透光心形眼镜”和“丘比特眼部”。原始用户文件与 v007 保留。

源文件：`asset-sources/link/v008/source.blend`；网页：`public/assets/link/v008/model.glb`。生成脚本为 `scripts/blender/refine_link_v008.py`，结构记录为 `revision-check.json`。面部包围范围通过 `scripts/blender/check_link_v008_face.py --no-render` 检查；在不扩大原丘比特包围范围、吊舱几何不变的条件下，沿用 v007 的吊舱净空下界，不宣称整机或摆动安全。
